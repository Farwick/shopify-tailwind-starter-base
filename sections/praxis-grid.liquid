<div class="ueber-uns-container twcss-p-16">
    {% for block in section.blocks %}
      {% assign input = block.settings %}
      <div class="!twcss-grid !twcss-grid-cols-3 !twcss-gap-6">
        <div class="twcss-col-span-2">
            <p >

        {{ input.text }}
        </p>
        </div>
            <div>
                <p>{{ input.kontakt }}</p>
                </div>

        </div>

    {% endfor %}
</div>

{% schema %}
{
  "name": "Über die Praxis",
  "blocks": [
    {
      "name": "Text",
      "type": "text",
      "settings": [
        {
          "type": "richtext",
          "label": "ueber-praxis",
          "id": "text"
        },
        {
            "type": "richtext",
            "label": "kontakt",
            "id": "kontakt"
          }
      ]
    }
  ]
}
{% endschema %}
